"use client";
import { useEffect,useState } from "react";
import { Download,HardDrive } from "lucide-react";
import { Button } from "@/components/ui/button";
import { Card,CardDescription,CardTitle } from "@/components/ui/card";
import { exportLocalData } from "@/lib/db/repositories/learning";
export function SettingsData(){const [online,setOnline]=useState(true);useEffect(()=>{const update=()=>setOnline(navigator.onLine);update();window.addEventListener("online",update);window.addEventListener("offline",update);return()=>{window.removeEventListener("online",update);window.removeEventListener("offline",update);};},[]);async function download(){const data=await exportLocalData(),url=URL.createObjectURL(new Blob([JSON.stringify(data,null,2)],{type:"application/json"})),anchor=document.createElement("a");anchor.href=url;anchor.download=`uas-study-hub-${data.exportedAt.slice(0,10)}.json`;anchor.click();URL.revokeObjectURL(url);}return <div><p className="text-sm font-bold uppercase tracking-[.18em] text-[#35796b]">Settings / Data</p><h1 className="mt-2 text-3xl font-bold">Tus datos, en este dispositivo</h1><div className="mt-7 grid gap-5 md:grid-cols-2"><Card><HardDrive className="text-[#35796b]"/><CardTitle className="mt-4">Persistencia local</CardTitle><CardDescription>IndexedDB · schemaVersion 1</CardDescription><p className="mt-4 font-semibold">Estado de red: <span data-testid="network-status" className={online?"text-[#26705f]":"text-[#9b5c12]"}>{online?"Online":"Offline"}</span></p></Card><Card><Download className="text-[#35796b]"/><CardTitle className="mt-4">Export Data</CardTitle><CardDescription>Descarga sesiones, evidencia, progreso y notas como JSON.</CardDescription><Button className="mt-5" onClick={()=>void download()}><Download size={17}/> Export Data</Button></Card></div></div>;}
